/**
 * Pass-through for plain text (Encryption disabled as requested)
 */
export function encrypt(text) {
  return text;
}

/**
 * Pass-through for plain text (Decryption disabled as requested)
 */
export function decrypt(text) {
  return text;
}
